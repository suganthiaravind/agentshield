boto3>=1.34.0
pydantic>=2.5.0
# SMARTSDK is a JPMC-internal package that wraps Google ADK; in a real repo
# this would resolve from the JPMC private index. The patterns in src/ match
# its public-facing surface (runner.run / runner.run_stream / runner.run_async).
smart-sdk>=0.1.0
google-adk>=0.1.0
